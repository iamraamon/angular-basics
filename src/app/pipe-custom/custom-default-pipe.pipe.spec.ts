import { CustomDefaultPipePipe } from './custom-default-pipe.pipe';

describe('CustomDefaultPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDefaultPipePipe();
    expect(pipe).toBeTruthy();
  });
});
